/* eslint-env browser */

/*
//STEP 1
function halfNumber(wholeNumber) {
    window.console.log("Half of " + wholeNumber + " is " + (wholeNumber / 2) + ".");  
}
halfNumber(12);
*/

/*
//STEP 2

function squareNumber(oneNumber) {
    window.console.log("The result of squaring the number " + oneNumber + " is " + Math.pow(oneNumber, oneNumber) + ".");
}
squareNumber(4);
*/

/*
//STEP 3
function percentOf(firstNumber, secondNumber) {
    var perCent = Number(firstNumber / secondNumber) * 100;
    window.console.log(firstNumber + " is " + perCent + "% of " + secondNumber);
}
percentOf(3, 4);

//STEP 4
function findModulus(numberOne, numberTwo) {
    var calculateModulus = numberOne % numberTwo;
    window.console.log(calculateModulus + " is the modulus of " + numberOne + " and " + numberTwo + ".");
}
findModulus(12, 5);
*/

/*
//STEP 5
function sumOfNumbers() {
    let numbers = [];
    for (i = 0; i <= 5; i += 1) {
    let value = window.prompt("Enter a number.", 0);
    numbers.push(+value);
        } 
    
    let sum = 0;
    for (let number of numbers) {
    sum += number;
}
    return sum;
    }

alert("The sum of your array of numbers is: " + sumOfNumbers() + ".");

*/